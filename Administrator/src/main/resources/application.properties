spring.datasource.url=jdbc:mysql://localhost:3306/backend?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=admin

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=none

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.username=bezbednostprojekat@gmail.com
spring.mail.password=projekatkraj
spring.mail.properties.mail.smtp.auth=true

spring.application.name=administrator-service
server.port=8763
eureka.client.service-url.default-zone=http://localhost:8761/eureka

security.oauth2.resource.jwt.key-value=as466gf

The format used for the keystore
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:Megatravel.p12
# The password used to generate the certificate
server.ssl.key-store-password=ozbiljnoTeskaSifra
# The alias mapped to the certificate
server.ssl.key-alias=megatravel
